export function createUpdateXML() {
    // Read csv

    // Loop through records, find the unique PUT/FILTER combo's and put them in a list with referentiepunten as subproperty

    // Loop through the newly created list

        // Fetch PUT xml from DOV, if available

        // If not available, fetch PUT xml from local files

        // Update PUT XYZ and add old XYZ to opmerking

        // Loop through referentiepunten

        // Update filter referentiepunten

        // Write to xml file
}